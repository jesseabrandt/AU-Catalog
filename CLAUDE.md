# CLAUDE.md — AU-Catalog

## North Star Framework

This project uses a north star framework. At session start, read:

1. [`north_star.md`](north_star.md) — the project vision
2. [`framework.md`](framework.md) — invariants, decision tree, session ritual,
   completion criteria

Follow the session start ritual in `framework.md` before touching files.

## Specs

Active and historical specs live in `docs/superpowers/specs/`. The session
ritual directs you to read the most recent one.

## modelrunnR Findings

This project doubles as a real-world stress test of the `modelrunnR` API.
Per framework invariant #7, API friction (awkward ergonomics, missing
features, forced workarounds) is logged — not routed around — to:

`/workspace/r-packages/modelrunnR/notes/AU-Catalog-findings.md`
